>--verbose

pro main(): Null = {
	foo("Hello, World")
}

fn foo(x: String): Null = {
	if (x == "Hello, World!") {
		println(x)
	} else {
		println("Hello, Psi!!")
	}
}

// TODO:
// Add utilities: SourceFile, Span, and collections
// Remake scanner and parser to use new Name classes
// Finish all of the compiler/ast/symbols dir
// Create AST Tree system
// Create Parser

